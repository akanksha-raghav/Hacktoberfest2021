Prerequisites:
Required instalations and running:
 - Node/ NPM
 - MongoDB

Run this command to install node modules
 - npm i 

Run the app
- node bin/www

Run the test
- mocha --timeout 10000 --exit

Run the coverage 
 - npm run coverage